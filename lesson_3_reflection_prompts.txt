When would you want to use a remote repository rather than keeping all your work
local?

    When you want to share your code and project to others, You would want to use a remote repository using github. 

Why might you want to always pull changes manually rather than having Git
automatically stay up-to-date with your remote repository?

    Because, many people work together on a project and make changes simultaneously. It would be a mess if git automatically pull and pushes all those changes without the confirmation.

Describe the differences between forks, clones, and branches.  When would you
use one instead of another?

    Forks is used to clone any repository hosted on github to our github profile. This repository is present only on remote. Clone is used to make a local copy of any repository hosted on github. Branches are used to keep track of all the commits and access them using the git log. We can use for, If we dont want to make a copy of repoitory in our computer and only edit it online. Colne is used if we want the files in our system.

What is the benefit of having a copy of the last known state of the remote
stored locally?

    Having a copy of last known state of remote stored locally is pretty helpful to know wheather you are in sync with the remote or not.

How would you collaborate without using Git or GitHub?  What would be easier,
and what would be harder?

    Generally, without using Git or Github, people would collaborate by emails to each other about the updations. It would be harder if many people try try to collborate and update the same files.

When would you want to make changes in a separate branch rather than directly in
master?  What benefits does each approach have?

    Fill in your answer here
